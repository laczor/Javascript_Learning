// --- Directions
// Given the rootNode node of a tree, return
// an array where each element is the width
// of the tree at each level.
// --- Example
// Given:
//     0
//   / |  \
// 1   2   3
// |       |
// 4       5
// Answer: [1, 3, 2]

// const Node = require('./node');

function levelWidth(rootNode) {

}

  // const rootNode = new Node(0);
  // rootNode.add(1);
  // rootNode.add(2);
  // rootNode.add(3);
  // rootNode.children[0].add(4);
  // rootNode.children[2].add(5);

 // console.log(levelWidth(rootNode)); 

module.exports = levelWidth;
